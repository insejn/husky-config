commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
pre-commit:
  parallel: true
  jobs:
    name: sass
    glob: "*.sass"
    # run: npx stylelint --custom-syntax postcss-sass {staged_files}
    run: npx stylelint --custom-syntax postcss-sass